<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or siteData.description }}">
  <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="alternate" href="{{ siteData.feed.path | url }}" type="application/atom+xml" title="{{ siteData.title }}">
  <link rel="alternate" href="{{ siteData.jsonfeed.path | url }}" type="application/json" title="{{ siteData.title }}">
  <title>{{ title }} | {{siteData.title}}</title>
</head>
  <body class="min-h-screen flex flex-col">
    <header class="bg-blue-700 text-white">
      <div class="container flex p-4 items-center justify-between">
        <a href="{{ '/' | url }}" class="text-2xl font-bold">{{ siteData.title }}</a>
        <ul class="grid grid-flow-col gap-4">
          {%- for entry in collections.all | eleventyNavigation %}
            <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
          {%- endfor %}
        </ul>
      </div>
    </header>

    <main class="container bg-blue-200 p-4 flex-grow">
        {{ content | safe }}
    </main>

    <footer class="bg-blue-700 text-white">
      <div class="container p-4">
        &#169; {{siteData.currentYear}} David Soards
      </div>
    </footer>
    {% set js %}
      {% include "js/main.js" %}
    {% endset %}
    <script>{{ js | jsmin | safe }}</script>
  </body>
</html>
